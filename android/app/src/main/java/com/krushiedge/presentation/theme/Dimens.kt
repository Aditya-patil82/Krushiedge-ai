package com.krushiedge.presentation.theme

import androidx.compose.ui.unit.dp

/**
 * KrushiEdge AI Design System — Spacing & Dimensions
 *
 * Consistent spacing creates visual rhythm.
 * Large touch targets (48dp minimum) for farmer accessibility.
 */
object KrushiDimens {

    // ── Spacing Scale ────────────────────────────────────
    val SpaceXxs = 2.dp
    val SpaceXs = 4.dp
    val SpaceSm = 8.dp
    val SpaceMd = 12.dp
    val SpaceLg = 16.dp
    val SpaceXl = 20.dp
    val SpaceXxl = 24.dp
    val Space3xl = 32.dp
    val Space4xl = 40.dp
    val Space5xl = 48.dp
    val Space6xl = 56.dp
    val Space7xl = 64.dp

    // ── Screen Padding ───────────────────────────────────
    val ScreenPaddingHorizontal = 20.dp
    val ScreenPaddingVertical = 16.dp

    // ── Corner Radius ────────────────────────────────────
    val RadiusSm = 6.dp
    val RadiusMd = 10.dp
    val RadiusLg = 14.dp
    val RadiusXl = 20.dp
    val RadiusFull = 100.dp

    // ── Elevation ────────────────────────────────────────
    val ElevationNone = 0.dp
    val ElevationSm = 1.dp
    val ElevationMd = 2.dp
    val ElevationLg = 4.dp
    val ElevationXl = 8.dp

    // ── Touch Targets ────────────────────────────────────
    val MinTouchTarget = 48.dp
    val ButtonHeight = 52.dp
    val ButtonHeightSmall = 40.dp
    val IconButtonSize = 48.dp

    // ── Component Sizes ──────────────────────────────────
    val BottomNavHeight = 64.dp
    val TopBarHeight = 56.dp
    val CardMinHeight = 72.dp
    val StatusIndicatorSize = 10.dp
    val ChartHeight = 200.dp
    val MapHeight = 280.dp

    // ── Borders ──────────────────────────────────────────
    val BorderThin = 1.dp
    val BorderMedium = 1.5.dp

    // ── Icon Sizes ───────────────────────────────────────
    val IconSm = 18.dp
    val IconMd = 22.dp
    val IconLg = 28.dp
    val IconXl = 36.dp
    val IconXxl = 48.dp
}
